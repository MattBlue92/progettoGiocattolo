language: python
python:
  - "3.7"
cache: pip
install:
  - pip install -r requirements.txt
script:
  - python -m unittest -v tests.itemsTest
  - python -m unittest -v tests.itemTest
  - python -m unittest -v tests.TestMumsDiscount
  - python -m unittest -v tests.TestStudentsDiscount